# Equipe Suite GitHub Defaults

Public, non-sensitive GitHub community defaults for repositories in the **equipe-suite** organization.

This repository intentionally contains only organization-wide contribution, pull request, issue and security templates.

Internal architecture, platform contracts, deployment rules and reusable private workflows remain in the private `equipe-suite/suite` governance repository.
